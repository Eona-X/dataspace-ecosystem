# Docker image configuration
docker.image.name=kafka-proxy-oidc-auth
docker.image.tag=latest

# Docker registry (leave empty for local builds only)
# docker.registry=your-registry.azurecr.io
docker.registry=

# Build configuration
go.version=1.23.0
