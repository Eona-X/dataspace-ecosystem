# Kafka Proxy Kubernetes Manager Configuration

# Vault Configuration
kafka.proxy.vault.addr=http://vault:8200
kafka.proxy.vault.token=root

# Check interval in seconds
kafka.proxy.check.interval=15

# Lock configuration
kafka.proxy.enable.lock=true
kafka.proxy.lock.file.path=/tmp/kubectl-deployer.lock

# Shared directory for queue files
kafka.proxy.shared.dir=/shared

# Kubernetes configuration
kafka.proxy.namespace=default
kafka.proxy.image=grepplabs/kafka-proxy:0.4.2

# OAuth2/OIDC Authentication Configuration
# Enable OAuth2 authentication for consumer proxies
kafka.proxy.oauth2.enable=false

# OAuth2/OIDC tenant ID
# Example: kafka.proxy.oauth2.tenant-id=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
kafka.proxy.oauth2.tenant-id=

# Application client ID for authentication
# Example: kafka.proxy.oauth2.client-id=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
kafka.proxy.oauth2.client-id=

# Static users for fallback authentication (comma-separated username:password pairs)
# Example: kafka.proxy.oauth2.static-users=admin:admin-secret,user1:password1
kafka.proxy.oauth2.static-users=

# Custom Docker image with OAuth2 plugins embedded
# Build this image using: ./gradlew :plugins:podmanize
kafka.proxy.oauth2.plugin-image=kafka-proxy-oidc-auth:latest

# Logging
web.http.port=8080
web.http.path=/api

# EDC Runtime
edc.runtime.id=kafka-proxy-k8s-manager